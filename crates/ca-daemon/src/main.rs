//! ca-daemon — local orchestrator daemon for claude_admin v1.
//!
//! Placeholder. M1 implements the UDS server, JSON-RPC framing,
//! SQLite state, and graceful shutdown.

fn main() {
    println!(
        "ca-daemon {}: not yet implemented (placeholder; see ca_v1 M1)",
        ca_lib::version()
    );
}
