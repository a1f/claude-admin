//! ca — CLI entry point for humans and architectors.
//!
//! Placeholder. M1 wires up the first subcommands: `ping`, `version`, `daemon`.
//! Architector-facing subcommands (`architect register`, `task spawn-coder`, etc.)
//! follow in M2 onward.

fn main() {
    println!(
        "ca {}: not yet implemented (placeholder; see ca_v1 M1)",
        ca_lib::version()
    );
}
